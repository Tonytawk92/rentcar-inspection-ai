# H2 Database
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Automatically create and drop the schema on startup for a clean slate
spring.jpa.hibernate.ddl-auto=create-drop

# Always run the data.sql script on startup
spring.sql.init.mode=always

# Defer datasource initialization to ensure schema is created before data.sql runs
spring.jpa.defer-datasource-initialization=true

jwt.secret=1yufZrsMrPQpd+sITbdrN8jtTxkQm7jSjEszrVJdqpU=

# Gemini API Key
gemini.api.key=AIzaSyAOKCqU6bUk0isaXyL75vWUpLCNBBq7RbY
gemini.model-name=gemini-2.5-flash
